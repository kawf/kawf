MAKE SURE "register_globals = On" is in your php.ini unless you have
configured your apache virtual host with "php_flag register_globals On"
(apache 1.3) or "php_admin_flag register_globals On" (apache 2.x)
